<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Główny Theme aplikacji -->
    <style name="Theme.QuizCraft" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Splash Screen customization -->
        <item name="android:windowSplashScreenBackground">@color/black</item> <!-- Albo inny kolor, jak chcesz -->
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
    </style>

    <!-- Theme dla profilu (jak potrzebujesz) -->
    <style name="Theme.QuizCraft.Profile" parent="Theme.QuizCraft">
        <!-- Customize profile-specific shit here -->
    </style>
</resources>
